<div class="row">
  <% @events.each do |event| %>
    <div class="card" style="width: 18rem; margin-right: 3em">
      <div class="card-body">
        <h5 class="card-title"><%= event.title %></h5>
        <p class="card-text"><%= event.description %></p>
      </div>
    </div>
  <% end %>
</div>
