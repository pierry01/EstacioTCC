<h1>Usuário: <%= @user.email %></h1>

<table class="table table-hover" style="margin-top: 4%" >
  <thead>
    <tr>
      <th scope="col">#</th>
      <th>Evento</th>
    </tr>
  </thead>
  
  <tbody>
    <% @user.events.each do |event| %>
      <tr>
        <th scope="row"><%= event.id %></th>
        <td><%= event.title %></th>
      </tr>  
    <% end %>
  </tbody>
</table>